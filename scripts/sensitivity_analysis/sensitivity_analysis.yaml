holdout:
  experiment_file: holdout.py
  experiment_tag: holdout
  num_chains: 1
  num_samples: 250
  args:
    rg: [[0, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50]] # we want to actually holdout them all at once, so nested list

default:
  experiment_file: default.py
  experiment_tag: main_results
  num_chains: 2
  num_samples: 1000
  num_warmup: 250
  args: {}